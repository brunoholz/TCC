/* Voltar a página para cima na imagem da empresa. */

var voltarParaCima = document.querySelector('#home');

voltarParaCima.addEventListener('click', function () {
    window.scrollTo(0, 0);
});

// ---------------------
